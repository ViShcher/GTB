name: Ping Telegram

on:
  workflow_dispatch:
  push:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send message to Telegram
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        run: |
          echo "Отправляю тестовое сообщение..."
          curl -v "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage" \
            --data-urlencode "chat_id=${TG_CHAT_ID}" \
            --data-urlencode "text=🔔 Тестовое сообщение из GitHub Actions (ветка: ${GITHUB_REF_NAME})"
